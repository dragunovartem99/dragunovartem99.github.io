name: Deploy

on:
    push:
        branches: ["main"]
    # Rebuilds daily so the repo list stays current
    schedule:
        - cron: "0 6 * * *"
    workflow_dispatch:

permissions:
    contents: read
    pages: write
    id-token: write

jobs:
    ci:
        uses: ./.github/workflows/ci.yaml

    pipes:
        needs: ci
        uses: dragunovartem99/pipes/.github/workflows/deploy-github.yaml@v1.2.0
        with:
            build-command: build
            dist-folder: ./dist
        secrets:
            build-env: '{"GITHUB_TOKEN": "${{ secrets.GITHUB_TOKEN }}"}'
